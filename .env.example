# Neon Postgres connection string (Vercel Marketplace integration).
# Required by lib/db/client.ts — the app will 500 on any DB-backed route
# without it. Not needed to run the crux reader.
DATABASE_URL=
